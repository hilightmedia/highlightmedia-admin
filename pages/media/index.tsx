import SiteLayout from "@/src/components/layout/siteLayout";
import Folder from "@/src/components/media";
const FolderPage = () => {
  return (
    <SiteLayout>
      <Folder />
    </SiteLayout>
  );
};

export default FolderPage;
